public class GenericObject {
    public static void genericObject(){
       map<string, integer> acc=new map<string, integer>();
        acc.put('Hitesh', 101);
          acc.put('hitesh', 102);
       system.debug(acc);
    }
}